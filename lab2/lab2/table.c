int    main (void){      int i, j, row, col;        printf ("Enter number of rows: "); //takes user input for rows  scanf ("%d", &row);    printf ("Enter number of columns: "); //takes user input for columns  scanf ("%d", &col);  for (i = 1; i <= row; ++i) //initializes i to be 1, ++i increments i until it   //is greater than number of columns. Then iteration stops.    printf ("     %d", i);    //displays value of i  printf ("\n"); //goes down to a new row  for (i = 1; i <= col; ++i){  //initializes i to be 1, ++i increments i by 1          printf("%d  ", i); //displays value of i      for (j = 1; j <= row; ++j) //initializes j to be 1, ++j increments j by 1        printf ("%#3d   ", i * j); //multiplies value of i times value of j        // for every position in the array      printf ("\n");      }      return 0;    }